@model IEnumerable<Aliquota.Domain.Models.Investimento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor_inicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor_final)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dt_entrada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dt_saida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor_inicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor_final)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dt_entrada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dt_saida)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Resgate" asp-route-id="@item.ID">Resgate</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
